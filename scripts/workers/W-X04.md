# W-X04：编写 12 个新模块的集成测试

## 背景
老板 20:42 强调"能多派别少派"。当前 12 个新功能只有编译验证，没有运行时测试。W-X04 给每个新模块写 1 个最小可运行的集成测试。

## 范围（新增文件）
所有测试文件放 `E:\DEMO\数据采集\DataupLoad\src\test\java\com\hikrobotics\solution\module\`，目录结构照搬 main。

## 必须测试的 12 个新模块
| # | 模块 | 测试场景 |
|---|---|---|
| 1 | YKScheduledTicketRenewer | mock time advance 49min → 反射验证 updateTicket 被调用 |
| 2 | WhiteListScheduledRunner | mock run() 5min → 验证 URI 白名单加载 |
| 3 | AlarmRetentionTask | insert 91 天前 + SOLVED 报警 → 触发任务 → 验证删除 |
| 4 | IgnoreExpireTask | insert ignore_alarm 已过期 → 触发任务 → 验证删除 |
| 5 | IgnoreAlarm PO/Mapper/Service | insert + selectById + update + delete |
| 6 | IgnoreAlarmController | mockMvc GET/POST/PUT/DELETE 4 接口 |
| 7 | YKServiceImpl alarmCount | mock alarmRecordMapper.selectCount → 验证 alarmCount 字段 |
| 8 | ClientOnlineChecker | mock ConcurrentHashMap + time advance → 验证调用 add(AlarmDTO) |
| 9 | AlarmRecordServiceImpl sendAlarmTextMessage | mock WS session → 验证 sendMessage 被调 |
| 10 | (预留) | — |
| 11 | (预留) | — |
| 12 | (预留) | — |

## 测试框架
- JUnit 5
- Mockito
- Spring Boot Test（@SpringBootTest with profile=test）
- H2 in-memory database（test profile）或 PG（连 5433，**只读 test schema**）

## 任务
1. 每个模块写 1 个 .java 测试类
2. 用 @DisplayName 标注中文测试名
3. 必须真跑通过（不是写死 assertTrue(true)）
4. 测试结果归档到 `docs/delivered/2026-07-22-W-X04-test-result.md`

## 约束
- ❌ **不准碰 production hik-java**（hik-java 现在没在跑，PM 要先确认能启动）
- ❌ **不准用 5433 PG 跑测试**（必须 H2 in-memory 或 test schema）
- ❌ **不准改 application-prod.yml**
- ✅ 测试只跑 mvn test / gradle test，不动 Spring Boot 主程序

## 回报格式
```
W-X04 完成：[success/partial/blocked]
测试类数：[数字]
覆盖模块：[1,2,3,...]
测试结果：[X/Y 通过]
归档文件：[路径]
```
