# W-X08：冒烟测试 SOP（老板 21:17 新规：只推最新 1 次，成功就停）

## 老板原话（21:17）
"先把p0做了，冒烟测试的时候注意 报警推送只推送最新的一次报警，1次成功后就停止测试报警。"

## 老板 SOP 三连（PM 严守）
1. **推之前** PM 必须在群里说 + 老板同意
2. **只推 1 次** 最新 1 条
3. **1 次成功后立刻停**（自动回滚 yk.enable=false，如果改了的话）

## 当前现场（21:17）
- hik-java PID 33532 已跑 13.5 分钟，440MB
- 80 端口 LISTEN ✅
- **yk.enable = false**（老板 18:01 熔断，PM 没擅自改）
- 当前链路：报警 POST `/client/data/alarm` → 落 PG（不推 yk，因为 enable=false）

## 任务

### Step 1：在群里跟老板说
PM 在群里写：
"老板，PM 请求推 1 条测试报警（line1A/F01，message='W-X08 冒烟测试'），走落 PG 不推 yk 链路，1 次后停。同意？"

### Step 2：老板同意后，PM 改 yk.enable = true（如果老板让推 yk）
**只有老板说"推 yk"才改**。如果老板说"只测落 PG"，保持 enable=false。

### Step 3：推 1 条
```powershell
$body = @{
    uuid = [guid]::NewGuid().ToString()
    lineNo = "line1A"
    faceNo = "F01"
    time = [DateTimeOffset]::UtcNow.ToUnixTimeMilliseconds()
    type = 1
    level = 1
    message = "W-X08 冒烟测试"
    alarmCount = 1
} | ConvertTo-Json

Invoke-WebRequest -Uri "http://127.0.0.1/client/data/alarm" -Method Post -ContentType "application/json" -Body $body -UseBasicParsing
```

### Step 4：验证 1 次成功
- 返回 200
- PG `alarm_record` 表有 1 条新数据（line1A + message='W-X08 冒烟测试'）
- 如果 yk.enable=true 推了 yk：看 error.log 里有没有 "push alarm to yk success"

### Step 5：立刻停
- **不管成功失败，只推 1 次**
- 如果改过 yk.enable=true：立刻改回 false
- 在群里跟老板说"测试完成"

## 约束（铁则 32：测试推送 SOP）
- ❌ **不准推超过 1 条**
- ❌ **不准循环推**
- ❌ **不准绕过 SOP 直接 POST**
- ✅ 推之前在群里说
- ✅ 推之后在群里说结果
- ✅ yk.enable=false 时不算"推测试"（只算落 PG）

## 失败时怎么办
- 启动失败 → 看 `log/DataupLoad/DataupLoad.log` 最后 30 行
- POST 失败 → 看 `log/DataupLoad/error.log`
- PG 没数据 → 看 HikariPool 状态
- 任何失败 → PM 群里汇报，**不要再推第二次**

## 回报格式
```
W-X08 完成：[success/partial/blocked]
群里老板同意：[Y/N，时间戳]
推了几条：[1/0]
返回码：[200/4xx/5xx]
PG alarm_record 新增：[Y/N，uuid]
yk 推送状态：[成功/失败/未推送]
老板最后指令：[原文]
```
