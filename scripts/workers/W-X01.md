# W-X01：恢复 80 端口 + 修 W-F01-A 启动失败（老板 B 方案）

## 紧急度：P0（80 端口当前 DOWN，报警落 PG 链路断了）

## 现状
- 老 hik-java PID 6560 已被 PM 杀掉
- 新 hik-java PID 28384 启动失败（YKScheduledTicketRenewer 注入 YKServiceImpl 失败）
- 当前 80 端口空着
- 现场 16 台工控机的报警 POST 会被拒（链路断了）

## 范围（只能改 1 个文件）
**改** `E:\DEMO\数据采集\DataupLoad\src\main\java\com\hikrobotics\solution\module\yingke\scheduled\YKScheduledTicketRenewer.java`

## 修复方案（老板 20:22 选 B 方案：1:1 仿 PSM 改 YKServiceImpl 内部）

### 最小修法（先恢复现场，后做 B 方案）
**推荐：方案 B（注入接口，不踩 JDK 代理坑）**
```java
// 把这一行：
@Autowired private YKServiceImpl ykService;
// 改成：
@Autowired private IYKService ykService;
```
同时加 import：
```java
import com.hikrobotics.solution.module.yingke.service.IYKService;
```

## 任务（PM 严守 SOP，4 步）

### Step 1：备份当前 classes
```powershell
Copy-Item -Path "E:\DEMO\数据采集\DataupLoad\target\classes" -Destination "E:\DEMO\数据采集\DataupLoad\target\classes-broken-step2" -Recurse -Force
```

### Step 2：改 W-F01-A 文件
- 改 YKScheduledTicketRenewer.java（方案 B，注入接口）
- 重新编译：
```powershell
cd E:\DEMO\数据采集\DataupLoad
.\jdk\bin\javac.exe -encoding UTF-8 -d target\classes -cp "lib\*" -sourcepath src\main\java src\main\java\com\hikrobotics\solution\module\yingke\scheduled\YKScheduledTicketRenewer.java
```

### Step 3：启动 hik-java
```powershell
cd E:\DEMO\数据采集\DataupLoad
cmd /c "start /B scripts\start-app.bat"
Start-Sleep 60
Get-NetTCPConnection -State Listen -LocalPort 80  # 必须能 ListEN
```

### Step 4：仅做 GET 验证（**不准 POST 报警**）
```powershell
# GET 测试接口（不会触发报警）
Invoke-WebRequest -Uri http://127.0.0.1/client/data/info -Method Get -UseBasicParsing
# 看返回不是 connection refused
```
**⚠️ 不要 POST /client/data/alarm** —— 老板 SOP 不准擅自推测试
就算 yk.enable=false 不推 yk，落 PG 的测试也按"测试推送"算，要老板同意

## 约束
- ❌ **不准擅自改 yk.enable**（必须保持 false）
- ❌ **不准 POST /client/data/alarm 推测试**（老板 SOP）
- ❌ **不准重启服务超过 1 次**（每次重启前 PM 在群里说）
- ✅ 可以 GET 测试接口（不会产生报警数据）
- ✅ 只能改 1 个文件（YKScheduledTicketRenewer.java）

## 报错时怎么办
- 启动失败 → 看 `log/DataupLoad/DataupLoad.log` 最后 30 行 → PM 群里汇报
- 80 端口没起来 → 立刻 PM 群里汇报

## 回报格式
```
W-X01 完成：[success/partial/blocked]
文件：[相对路径]
方案：[A 或 B]
启动 PID：[数字]
80 端口：[up/down]
GET 测试：[返回码]
备份目录：[target/classes-broken-step2]
```
