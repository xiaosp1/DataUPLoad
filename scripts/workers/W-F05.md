# W-F05：大屏 ws 实时刷报警

## 范围（最多 2 个文件）
1. 改 `E:\DEMO\数据采集\DataupLoad\src\main\java\com\hikrobotics\solution\module\alarm\service\impl\AlarmRecordServiceImpl.java`
2. 复用现有 `WebSocketHandler`（framework-starter）

## 任务
报警落 PG 后，1 秒内通过 WebSocket 推到大屏（/ws/alarm 客户端）。

## 依据
PSM 反编译 `AlarmRecordServiceImpl.sendAlarmTextMessage` 字节码：
- 调 `webSocketHandler.sendMessage(topic, message)`
- topic 应该是 "alarm" 或 "screen"

## 实现要点
在 `AlarmRecordServiceImpl.add()` 方法里，**save(alarm) 之后**加：

```java
// === PSM 大屏 ws 实时刷报警（PM 2026-07-22 18:35 W-F05）===
if (webSocketHandler != null) {
    webSocketHandler.sendMessage("alarm", JSON.toJSONString(alarm));
}
```

注意：
- `webSocketHandler` 已经是 AlarmRecordServiceImpl 的字段（之前反编译看到）
- JSON 用 fastjson（项目里已经有）
- URI 走 `/ws/alarm`（看 framework-starter）

## 验证
- 编译通过
- 重启后推测试报警（⚠️ 老板同意才能推），浏览器开 /ws/alarm 端看 1 秒内收到

## 约束
- **不准擅自重启服务**
- **不准擅自改 yk.enable**
- **不准擅自推测试报警**
- 改完编译验证后回报，等老板指令

## 回报格式
```
W-F05 完成：[success/partial/blocked]
文件：[相对路径]
编译：[通过/失败]
代码 diff：[关键 3-5 行改动]
风险：[推测试才会触发，需老板同意]
建议测试：[怎么验证]
```
