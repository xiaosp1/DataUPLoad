# Local Tool Notes

- Codex CLI 全局可用：`codex` (codex-cli 0.142.x)
- 派工命令：`codex exec -C <dir> --skip-git-repo-check -s workspace-write "<prompt>"`
- 项目根：`E:\DEMO\数据采集`
- 海康领域文档/原型：`docs/domain/海康视觉接口/`
- 探查脚本：`scripts/probe-edge.ps1`（用法见 scripts/README.md）
- .NET CLI：`dotnet`（全局安装，版本随 VS）
- Git：可用
- 编码：所有文件统一 UTF-8（无 BOM），中文
- PowerShell：Gateway 进程已继承 Bypass 策略，npm shims 可用
