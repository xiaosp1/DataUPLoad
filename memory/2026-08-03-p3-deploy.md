# Memory — 2026-08-03 P3 车间部署现场排查（续，含 23:11 中断点）

## ⚡ 会话中断点（23:11，老板要开新会话继续）

### 当前状态
- P3 部署包已拷到 D:\DataupLoad\P3-deploy\，install.bat 跑通（装 PG + 建库 + 导入表 + 启后端 + 自启）
- 后端 8080 **正在运行**（老板手动用 java.exe 启动的 cmd 窗口，不能关）
- PG 5432 **正在运行**（老板手动 pg_ctl start 起的，会话级，重启机器会丢）
- Web 端**能登录**（super_admin / Abc12345），报警数据有
- **产线实时数据没有**——两个 bug 待修：
  1. **line 表中文编码错乱**（psql GBK 客户端插入的中文 lineNo 是乱码，后端查不到 → 20204）
  2. **status_record 缺 line_id 列**（V1.19 ALTER 被 GBK 编码错打断 → status 上报 500）

### 待老板执行（新会话第一步）
```
# ① 查 line 表实际存的值（预期看到乱码，证明编码问题）
D:\DataupLoad\P3-deploy\pg\postgres\bin\psql.exe -h 127.0.0.1 -p 5432 -U postgres -d intco -c "SELECT id, name, line_no, face_no FROM line ORDER BY id LIMIT 5;"

# ② 补 status_record.line_id（修 status 500）
D:\DataupLoad\P3-deploy\pg\postgres\bin\psql.exe -h 127.0.0.1 -p 5432 -U postgres -d intco -c "ALTER TABLE status_record ADD COLUMN IF NOT EXISTS line_id int NULL;"
```

### 已确定的修复方案（新会话执行）
**修复 line 表中文乱码**：写 UTF-8 SQL 文件（psql -f 读 UTF-8 文件不乱码，`-c` 命令行走客户端编码会乱）：
```
# 在 P3 机器建 fix_line.sql（UTF-8 编码）：
DELETE FROM line;
INSERT INTO line (name, line_no, face_no, client_no, color) VALUES
('PVC3厂1线','PVC3厂1线','A1','PVC3厂1线','#00c8ff'),
('PVC3厂1线','PVC3厂1线','A2','PVC3厂1线','#00c8ff'),
('PVC3厂1线','PVC3厂1线','B1','PVC3厂1线','#00c8ff'),
('PVC3厂1线','PVC3厂1线','B2','PVC3厂1线','#00c8ff'),
... 1~8线 × A1/A2/B1/B2 = 32 行 ...
;
# 执行：psql -h 127.0.0.1 -p 5432 -U postgres -d intco -f fix_line.sql
```
或 PM 直接生成 fix_line.sql 文件发给老板拷贝。

### 产线上报真实格式（日志实证，2026-08-03 22:52~22:57）
- **8 条产线，每条 4 面**：lineNo=`PVC3厂1线`~`PVC3厂8线`，faceNo=`A1/A2/B1/B2`
- 上报地址：`http://192.168.137.180:8080/client/data/detect`（产线 IP 192.168.135.52/58/60/63/65 等已连上）
- 字段：`todayData{totalNum,ngNum,statisticTime,defects[]}` + `realTimeData{total,ngCount,removeTotal,removeFail,efficiency,totalNgRate,occupancy,occupancyRate,startTime,defects[]}`
- defects: `{count,type,showFlag}`（type 如 未脱模/大撕破/腕部刮破/轻微油污/小杂点 等 29 种）
- 每 5s 上报；`/client/data/status`（含 device_no 如 DA1129620）+ `/client/data/alarm` 也在报
- **注意**：defect_type 表查询返回 0 行（`SELECT ... FROM defect_type WHERE category=?` → Total: 0）——缺陷类型配置表可能也是空的（GBK 导入时 INSERT 失败？），报警规则可能不生效，待查

### 已修复/已完成（P3 现场）
- PG 安装 + intco 库 + 19 表（account/role/white_ip/defect_record/line/alarm_record/status_record/...）
- white_ip：127.0.0.1 + 192.168.137.180 + 192.168.135.50~65 + *.*.*.*（19 条）
- role：super_admin/admin/operator 3 条（V0.1 INSERT 手动补）
- account：super_admin / Abc12345（手动 INSERT，hash=`$2a$10$8waDc...` 验证过匹配 bcrypt(sha256(Abc12345))）
- line：32 行（但中文可能是乱码，待查/待修）
- 登录 ✅ 报警 ✅ 线别配置页面显示 ✅

### install.bat 已修复的问题（git 已推）
1. **闪退**：UTF-8 中文 → 纯 ASCII 英文版
2. **PG_INSTALL 路径缺分隔符**：`%PG_ROOT%postgres` → `%PG_ROOT%\postgres`
3. **JDK 选择 bug**：hik-java.exe 优先（裸机启动失败）→ java.exe 优先
4. **Flyway baseline 1.20 跳过建表**：install.bat 建库后自动导入 V0.x（jar 提取）+ V1.x（server/sql）全部 SQL

### ⚠️ install.bat 遗留问题（新会话待修）
1. **PG 没注册 Windows 服务**：现在 `pg_ctl start` 是会话级，重启机器 PG 丢失 → 要改成注册服务（`pg_ctl register` 或安装时自动注册 + `net start postgresql-x64-14`）
2. **SQL 导入编码**：install.bat 的 psql -f 导入如果 psql 客户端编码 GBK，V0.x/V1.x 里中文 INSERT 会乱 → 需在导入前 `SET client_encoding TO 'UTF8'` 或 `chcp 65001` 或 psql 加 `PGCLIENTENCODING=UTF8`
3. **autostart.bat 的 JDK**：Startup 文件夹生成的 DataupLoad.bat 要确认用 java.exe（新版 install.bat 已改，但老板现场跑的是旧版，自启文件可能是旧的）
4. **后端启动**：现场用 java.exe 手动起的窗口，重启后要能自启（Startup 里的 bat）

### 关键技术事实（P3 现场复用）
- psql 路径：`D:\DataupLoad\P3-deploy\pg\postgres\bin\psql.exe`
- 后端启动：`cd /d D:\DataupLoad\P3-deploy\server && jdk\bin\java.exe -cp "lib\*;target\classes" -Dfile.encoding=UTF-8 -Dspring.config.location=./config/ -Dspring.config.name=application -Dserver.port=8080 com.hikrobotics.solution.Application`
- PG 启动：`pg\postgres\bin\pg_ctl.exe start -D pg\postgres\data -l pg\postgres\data\pg.log`
- 后端日志：`D:\DataupLoad\P3-deploy\server\log\DataupLoad\DataupLoad.log`（error.log 是 ERROR 级）
- 产线数据表：line_day_record（日汇总）/ defect_record（检测明细）/ status_record（状态，缺 line_id 列待补）
- **编码教训**：Windows cmd 下 psql 默认 GBK 客户端编码，`-c "中文"` 插入会乱码；**必须用 UTF-8 SQL 文件 + `psql -f`**，或先 `SET client_encoding TO 'UTF8'`
- **P3 线命名**：`PVC3厂{1..8}线` × face `A1/A2/B1/B2`（**不是** Line1A 风格！PM 之前建 Line1A/A1 全错）

### 排查历史（时间线）
- 22:02 install.bat 首次跑 → 闪退（编码）→ 22:10 纯 ASCII 版
- 22:14 手动 java.exe 启动 → Spring Boot 起来但 8080 拒绝连接
- 22:26 netstat 无 8080 → error.log 显示 `white_ip 关系不存在` → 空库没表
- 22:30 PG 没跑（会话级）→ 手动 pg_ctl start
- 22:33 导表（V0.1/V0.2/V1.0~V1.19）→ 大量 GBK 编码错（COMMENT 丢）+ V1.7 white_ip 建表失败
- 22:37 登录页出来但密码错误 → account 表空（V0.1 INSERT 被编码错吃掉）
- 22:40 手动 INSERT super_admin（`\$` 转义错，length=63 含反斜杠）→ 22:41 修正 hash（length=60）
- 22:42 role 表空 → 403 → 手动 INSERT 3 条 role → 22:45 登录成功
- 22:45 报警有数据但产线/实时无
- 22:52~22:57 日志实证：产线在报但 lineNo=`PVC3厂X线` 未注册 → 20204
- 22:56 建 Line1A~8B（16 条，错）→ 22:58 换 PVC3厂X线 × A1/A2/B1/B2（32 条）→ 但 psql 中文可能乱码
- 23:03 line_day_record=0（数据没进）→ 23:06 日志：status_record 缺 line_id（V1.19 ALTER 失败）
- 23:08 curl 实测：`PVC3厂1线/A1` → 20204（line 表中文乱码查不到）
- 23:11 老板要求记录，开新会话继续

### 老板环境
- P3 工控机：Windows 10 19045.6466，Administrator 用户
- 部署目录：D:\DataupLoad\P3-deploy\
- 后端 PID：手动 java 窗口（未记 PID）
- 本机（PM 侧）：E:\DEMO\数据采集，后端 8080 PID 27736（本机业务不受影响）

## 明日/新会话待办清单
1. [ ] 修 line 表中文乱码（UTF-8 SQL 文件重插 32 行）
2. [ ] 补 status_record.line_id（ALTER TABLE）
3. [ ] 查 defect_type 表是否空（报警规则配置）
4. [ ] 验证 line_day_record 有数据 → 前端实时页刷新显示
5. [ ] install.bat：PG 注册服务（重启不丢）+ psql 导入强制 UTF-8
6. [ ] 确认 autostart.bat 用 java.exe
7. [ ] 桌面端 Manager exe 在 P3 实测
8. [ ] 老板验收：实时数据/大屏/报警推送全链路
