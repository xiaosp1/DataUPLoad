你是 PSM 审计 + Java 开发 worker。任务：**W-DET-01** — 补全 IDefectDayRecordService 8 个缺失方法。

## 任务背景
参考审计报告: `E:\DEMO\数据采集\docs\audit\2026-07-24-detect-audit.md` §重点问题 Top 1
参考 PSM 反编译产物: `E:\DEMO\数据采集\docs\domain\海康大屏逆向\PSM\server\decompiled\com\hikrobotics\solution\module\detect\service\IDefectDayRecordService.java`
当前 DataupLoad 接口: `E:\DEMO\数据采集\DataupLoad\src\main\java\com\hikrobotics\solution\module\detect\service\IDefectDayRecordService.java`
当前 DataupLoad 实现: `E:\DEMO\数据采集\DataupLoad\src\main\java\com\hikrobotics\solution\module\detect\service\impl\DefectDayRecordServiceImpl.java`

## 8 个方法（接口签名 1:1 抄 PSM + Impl 实现）

| 方法 | 返回类型（参考 PSM） | 说明 |
|---|---|---|
| `addLineDayRecord(records)` | void | 批量 upsert（hour 切片） |
| `listByAttribute(query)` | List<DefectDayRecord> | 多条件列表 |
| `listByStartTime(time)` | List<DefectDayRecord> | 小时粒度查询 |
| `searchDefectCount(time, line, face, defects)` | List<DefectCountDTO> | 缺陷计数（hour 粒度） |
| `searchDefectCount(start, end, line, face, defects)` | List<DefectCountDTO> | 缺陷计数（范围） |
| `listByLineAndTime(line, time)` | List<DefectDayRecord> | 单产线+小时 |
| `removeByType(type)` | int | 按 type 删除 |
| `listBetween(start, end)` | List<DefectDayRecord> | 时间段查询 |

## 工作要求
- 接口签名 1:1 对齐 PSM（命名/参数/返回类型）
- Impl 实现用 MyBatis-Plus：LambdaQueryWrapper / IService.saveBatch / baseMapper.selectList 等
- DTO 类型用 DataupLoad 已有的：`DefectCountDTO` (在 line.dto 包) 或者 PSM 同款（看 PSM 反编译）
- 不要新增文件，全部在现有 IDefectDayRecordService.java + DefectDayRecordServiceImpl.java 内补全
- 编译验证：`cd E:\DEMO\数据采集 && javac -encoding UTF-8 -d X:\DataupLoad\target\classes -cp "X:\DataupLoad\target\classes;X:\DataupLoad\lib\*" -sourcepath DataupLoad\src\main\java DataupLoad\src\main\java\com\hikrobotics\solution\module\detect\service\impl\DefectDayRecordServiceImpl.java`

## 输出
完成后写报告到 `E:\DEMO\数据采集\docs\work-orders\W-DET-01-report.md`：
- 改动的文件
- 8 个方法的方法签名 + 实现要点
- 编译结果
- 已知限制

不要修改其它模块，不要推 git。