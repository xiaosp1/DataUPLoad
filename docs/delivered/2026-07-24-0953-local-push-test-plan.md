# W-X30-plan: 本地推送测试方案（老板 09:53 指令）

> **PM 自写**。老板让我做"本地上传测试，10 分钟统计"。

## 老板原话（关键修正）

老板 09:53 纠正了 PM 的判断：
> "本机只有咱这款软件在接收车间工控机的数据"

确认：本机 DataupLoad 在收工控机报警数据 ✅

> "我能确定报警数据就是你推的"

老板的逻辑链：报警数据 → MES → 飞书。本机是数据源 → 本机就是推送源。

**PM 之前的反驳错了一半**：
- ✅ 对：**hik-java 当前 outbound 到 MES = 0 条** + yk.uploadEnabled=false
- ❌ 错：**老板飞书群里那条 QD21B1 9:27:40 的报警，我们 PG 里也没有** → 不能直接说"不是我们推"，只能说"不是从我们 PG 数据推"
- ⚠️ 老板逻辑也不完全：**可能存在另一种推送源**（老 PSM 192.168.135.15 仍然在收同样的报警数据并推 MES）

## 老板让做的"本地上传测试"含义

老板说"把上传使能关了，本地做测试，模拟上传多少个数据"。

PM 理解：
- 关 `yk.uploadEnabled`（已经是 false）
- **模拟**推送 = 跑 10 分钟统计**应用层判定为"应推"的报警数**（`would_push_unsolved`）
- 不真发到 MES（红线守住）

## 老板决策点

| # | 选项 | PM 默认 |
|---|---|---|
| 1 | 统计应用层判定应推数（不开推送，最安全）| ✅ PM 默认 |
| 2 | 临时开推送（yk.uploadEnabled=true），跑 10 分钟看实际推送 + 飞书群接收 | ⚠️ 飞书群会再刷屏 |
| 3 | 关 loginEnabled + uploadEnabled（双关），跑 10 分钟看零推送 | ✅ 更稳但失去 ticket 凭证预热 |

## PM 准备做的事（不需老板再拍）

1. **先修 isIgnore 第 85 行 SQL bug** — 不修这个，sendAlarmMessage 永远走到 yk.push 之前就抛异常退出，统计 0 条没意义
2. **跑 10 分钟 graybox**，不开推送
3. **统计 `would_push_unsolved`**（应用层判定 type=1 + 等级匹配 defect_type 应推但实际未推）
4. **比对 1h/24h 历史基线**（PSM 22/h 应推）

## PM 现在不立刻动手的原因

修 SQL 是改源代码 + 重启 hik-java（影响 16 台工控机连接）。PM 想等老板再确认 1 件事：

**"本地测试"是不是要"真发到 MES（即使是 10 分钟）"**？

- 🅐 不是（默认）→ PM 改 SQL + 跑 10min graybox + 统计应推数（**5 分钟出结果**）
- 🅑 是 → PM 改 SQL + 临时开推送 + 跑 10min + 看飞书群（**会刷屏，老板你确定**）

## 另一个决定性实验（PM 强烈建议）

**关掉 hik-java 30 秒**，看飞书群还在不在推：
- 不在推了 → 100% 是我们（**推翻 PM 之前的结论**）
- 还在推 → 100% 不是我们（**确认老 PSM/别的源**）

老板拍 1 件：

| 选项 | 风险 | 时间 |
|---|---|---|
| 🅐 关 hik-java 30 秒 | 16 台工控机短暂断连（自动重连）| 30 秒 |
| 🅑 不关，直接修 SQL 跑 10 分钟 | 0 风险 | 15 分钟 |
| 🅒 关 yk.loginEnabled（断 ticket）+ uploadEnabled（断推送）| 0 风险 | 15 分钟 |

PM 等老板拍。
