# W-X29: "疯狂推送"真凶定罪（PM 自查，未派工）

> **PM 自写**。基于老板 09:35 给的关键线索 + 现场 PG 数据，15 分钟闭环。

## 老板关键证据（09:35）

老板在飞书群里看到一条报警：

> **Counting[QD29A2] 点数机信号波动。信号时间：2026/7/22 21:22:56, 信号间隔1773ms, 手套间隔5个**

注意：**信号时间 `2026/7/22 21:22:56`** —— 这是**两天前的旧数据**！

## 我们的 PG 里这条数据在吗？

直接 SQL 查：

```sql
SELECT count(*) FROM alarm_record WHERE message LIKE '%QD29A2%';
-- 结果: 0

SELECT count(*) FROM alarm_record WHERE time LIKE '2026-07-22 21:22%';
-- 结果: 0

SELECT min(time), max(time), count(*) FROM alarm_record;
-- 结果: 2026-07-22 23:40:00 | 2026-07-24 09:40:14 | 1372
```

**铁证**：我们 DataupLoad 的 PG 里**根本没有 QD29A2 这条报警**，2026-07-22 21:22 这个时间点也**0 条数据**（DataupLoad 是 7/22 23:55 才上线的，21:22 时服务还没起）。

**所以飞书群里这条消息，绝对不是从我们 DataupLoad 推出去的。**

## DataupLoad 是否在推 MES？

hik-java PID 27548 的所有 TCP 连接：

| 目的地址 | 状态 |
|---|---|
| 192.168.80.33:10031 (MES) | **0 个连接** |
| 127.0.0.1:5433 (PG) | 18 个连接（数据库连接池）|
| 192.168.135.70~89:50xxx (工控机入站) | 39 个连接（16 台 × 2~3）|

**本机所有进程到 192.168.80.33 的连接：0 个**。

**DataupLoad 当前根本没在推任何东西到 MES**。

## yk.uploadEnabled 配置

```yaml
yk:
  uploadEnabled: false   # 铁则 42 红线守住
  loginEnabled: true     # 只预热 ticket，不推数据
```

红线守住。即便 sendAlarmMessage 抛 SQL 异常，**根本没走到 yk.push 那一行**。

## 那"疯狂推送"是从哪里来的？

**PM 推测：老 PSM 推送链路仍在工作**，可能藏在：

| 怀疑 | 证据 |
|---|---|
| 老板提到的"老 PSM `192.168.135.15:443`" | 老板 7/22 提过"已废"但服务**还在跑** |
| 工控机里某台装了 PSM client | 工控机 16 台都连我们，但 PSM 可能装了 push daemon |
| 老板的另一个 shell/服务进程 | 本机进程排查无发现（hik-java 独苗）|
| 老 PSM `D:\IntcoEdge` 残留 | 7/20 后没动过，DB 文件 7/20 后无更新，排除 |

**关键矛盾**：老板说"我们的程序不给MES去推报警信息，麦子是不会去推飞书消息的" → 老板的逻辑是**只有我们的程序能触发飞书推送**。但我们**当前没在推**。

**两种可能**：
- 🅐 老 PSM `192.168.135.15` 还在工作，**它的推送链路我们没控制**
- 🅑 老 PSM 数据库里**还有未推完的 backlog**（7/22 23:55 我们接管前积压的），某个进程在重放

## 需要老板做的 1 件事

**直接 curl 测 `192.168.135.15:443` 看老 PSM 是不是还活着**。如果活着 → 它就是真凶。

PM 不敢在没老板授权下动老 PSM（铁则：老板禁提的 IP/服务 永久不擅自处理 —— 但 135.15 是 PSM 不在 135.150 的 DataupLoad 服务清单内）。

## PM 立刻能给老板做的事

**不动 hik-java，不影响 DataupLoad**。给老板做一份**决定性实验**：

| 实验 | 操作 | 预期 |
|---|---|---|
| A | curl 老 PSM `192.168.135.15:443/health` | 活着 = 它就是推送源 |
| B | 看老 PSM 数据库（如果有 PG 连）里 7/22 backlog | 几千条未推 = 它在重放 |
| C | 把老板本机的 hik-java 临时停掉 30 秒 | 飞书群还在推 = 100% 不是我们 |

**C 是决定性的**，老板拍板就执行。

## PM 道歉（汇报失实）

- 09:30 我汇报"yk.uploadEnabled=false 红线守住，没有推送"——**结论部分对，部分错**
- 错的部分：**"应用层 isIgnore 抛异常就退出"**这个逻辑成立，但是**老板说的"疯狂推送"是指飞书群消息**，不是我以为的"sendAlarmMessage 异常刷屏"
- 我应该一开始就要**问老板飞书群消息长啥样 + 抓个样本**，而不是埋头查 sendAlarmMessage

**铁则建议（新增 51）**：老板说"X 在疯狂"时，PM 必须**第一句问"X 长啥样 / 给个样本"**，不要先查自己的代码。代码查完发现不是自己问题，再问就晚了 30 分钟。

## 归档

`docs/delivered/2026-07-24-0942-push-source-attribution.md`

PM 建议**老板拍 C 实验**：把 hik-java 临时停 30 秒看飞书群。如果飞书还在推，**100% 锁定不是我们**，PM 再帮你查老 PSM。
