# W-X13a — EdgeHost C# 端 yk.enable 字段（拆细·步骤 1/3）— 执行报告

> **Worker**: W-X13a
> **执行时间**: 2026-07-23 01:18 ~ 01:25 (GMT+8)
> **派工单**: `scripts/workers/W-X13a.md`
> **结果**: ✅ PASS（3/3 步骤完成，build 0 error，test 248 pass / 0 fail）

---

## Step 1 — 读现有 appsettings.json（3 min）

### 1.1 路径修正（重要）

工单里写的路径是 `E:\DEMO\数据采集\src\IntcoEdge\appsettings.json`，**该路径不存在**。
实际 C# EdgeHost 项目目录是 `src\IntcoEdge.EdgeHost\`（注意多了 `.EdgeHost` 段）。

执行的命令与命中：
```powershell
Get-ChildItem -Path E:\DEMO\数据采集\src -Recurse -Filter "appsettings*.json"
# → E:\DEMO\数据采集\src\IntcoEdge.EdgeHost\appsettings.json
# → E:\DEMO\数据采集\src\IntcoEdge.EdgeHost\appsettings.Development.json
# （其他命中都是 bin/Debug/net8.0/ 下编译产物，不动）
```

> PM 注意：建议下个工单 W-X13b/c 路径都按 `IntcoEdge.EdgeHost` 写。

### 1.2 现有 yk 相关结构

读 `src/IntcoEdge.EdgeHost/appsettings.json` 后现状：

| 段 | 路径 | 关键字段 |
|----|------|---------|
| `Logging` | 顶层 | `LogLevel.Default/...` |
| `Alarm` | 顶层 | `_comment`, `Config[]` |
| `IntcoEdge` | 顶层 | `Port/DbPath/...`, `YingkeGateway.{Url, Username, Password, WorkshopCode, InvOrgId, TimeoutMs, RetryCount, TicketCacheMinutes, Enabled}` |
| **`yk`** | **不存在** | **需要新建** |

⚠️ **重要发现**：现有的 `IntcoEdge.YingkeGateway.Enabled = true`（W-A6 加的英科网关配置）。
按工单"严禁"清单第 1 条 ❌ 不要删现有 yk 配置字段（只加不删），**此字段保留不动**，
由 W-X13b 在代码层 `YingkeServiceImpl.cs` 做 `if (yk.enable == false) return;` 短路。

---

## Step 2 — 加 yk.enable 字段（5 min）

### 2.1 决策

- **新顶层段** `yk`（因为没有现成 yk 段）
- 放在 `AllowedHosts` 之后、`Alarm` 之前（保持顶层段按"通用 → 业务"顺序排列，与现有 `Logging/AllowedHosts/Alarm/IntcoEdge` 风格一致）
- 注释风格：用 `_comment`（与 `Alarm._comment` / `IntcoEdge.YingkeGateway._comment` 一致，不要用 `//` JSON 不支持）
- 默认值：`false`（老板 2026-07-22 23:23 拍板）

### 2.2 改动 diff

```diff
   "AllowedHosts": "*",
+  "yk": {
+    "_comment": "yk.enable = false: 老板 2026-07-22 23:23 拍板永久熔断，禁止推 yk。W-X13a 加字段（默认 false 防意外）。W-X13b 在 YKService.cs 加 3 道熔断；W-X13c 写 ADR-0006。注：IntcoEdge.YingkeGateway.Enabled 保留不动（工单严禁删字段），由 W-X13b 在代码层短路。",
+    "enable": false
+  },
   "Alarm": {
```

### 2.3 JSON 合法性

```powershell
Get-Content -Raw ...\appsettings.json -Encoding UTF8 | ConvertFrom-Json
# → JSON_OK（无报错）
```

### 2.4 解析验证

```powershell
$cfg = Get-Content ...\appsettings.json -Raw -Encoding UTF8 | ConvertFrom-Json
$cfg.yk.enable         # → False
$cfg.yk._comment       # → "yk.enable = false: 老板 2026-07-22 23:23 拍板永久熔断..."
```

---

## Step 3 — build + test 验证（7 min）

### 3.1 build

```powershell
dotnet build E:\DEMO\数据采集\src\IntcoEdge.EdgeHost\IntcoEdge.EdgeHost.csproj -c Release
```

输出（控制台 GBK 解码 UTF-8 致乱码，关键信号清晰）：
```
  IntcoEdge.Common -> ...\bin\Release\net8.0\IntcoEdge.Common.dll
  IntcoEdge.Db -> ...\bin\Release\net8.0\IntcoEdge.Db.dll
warning CS0162: 检测到无法访问的代码 ...\Services\Alarm\YingkeServiceImpl.cs(133,9)
  IntcoEdge.EdgeHost -> ...\bin\Release\net8.0\IntcoEdge.EdgeHost.dll

已成功生成。
    1 个警告
    0 个错误

已用时间 00:00:21.84
```

✅ **BUILD SUCCESS**（0 错误）

⚠️ **1 warning `CS0162`**：位置 `Services\Alarm\YingkeServiceImpl.cs:133`。
含义是"检测到无法访问的代码"，说明现有代码已经有一段 `if (...) return;` 短路分支，
后面真推 yk 的代码 unreachable。这是 W-X13b 的工作面（按工单严禁清单 ❌ 不要修改 `YkService.cs` 等 W-X13b 范围代码），
**本工单不动**，留给 W-X13b 处理（工单里说"YingkeServiceImpl.cs 加 3 道熔断"）。

### 3.2 test

```powershell
dotnet test E:\DEMO\数据采集\src\IntcoEdge.Tests\IntcoEdge.Tests.csproj -c Release --nologo
```

输出关键行：
```
已通过! - 失败:     0，通过:   248，已跳过:     0，总计:   248，持续时间: 4 s - IntcoEdge.Tests.dll (net8.0)
```

✅ **248 通过 / 0 失败 / 0 跳过**（零回归）

### 3.3 dotnet 版本

```powershell
dotnet --version
# → 8.0.423
```

---

## 验收命令复跑（PM 验收脚本）

```powershell
# 1. yk.enable 字段存在（注释命中）
Select-String -Path E:\DEMO\数据采集\src\IntcoEdge.EdgeHost\appsettings.json -Pattern 'yk.*enable'
# → 第 10 行命中 _comment 含 "yk.enable"

# 2. 真正配置值（PM 推荐额外跑）
$cfg = Get-Content E:\DEMO\数据采集\src\IntcoEdge.EdgeHost\appsettings.json -Raw -Encoding UTF8 | ConvertFrom-Json
$cfg.yk.enable         # → False
$cfg.yk._comment       # → 命中

# 3. build
dotnet build E:\DEMO\数据采集\src\IntcoEdge.EdgeHost\IntcoEdge.EdgeHost.csproj -c Release | Select-Object -Last 10
# → "0 个错误"

# 4. dotnet 版本
dotnet --version
# → 8.0.423
```

---

## 关键结论

| 项 | 结果 |
|----|------|
| 新字段 `yk.enable = false` | ✅ 已写入 |
| 现有字段 0 改动 | ✅ 校验通过（`IntcoEdge.YingkeGateway.Enabled=true` 保留） |
| JSON 合法 | ✅ `ConvertFrom-Json` 无报错 |
| dotnet build | ✅ 0 error / 1 warning（W-X13b 范围，留给下个工单） |
| dotnet test | ✅ 248 pass / 0 fail |
| 工单严禁清单 5 条 | ✅ 全部遵守（不改其他配置 / 不删字段 / 默认 false / 不启 C# / 不改 YKService.cs） |

---

## 后续工单交接（PM 必看）

- **W-X13b**：去 `src/IntcoEdge.EdgeHost/Services/Alarm/YingkeServiceImpl.cs` 加 3 道熔断
  - 第 1 道（配置层短路）：`if (config.yk.enable == false) { log.Info("yk.enable=false skip"); return; }`
  - 第 2 道（ticket null 短路）
  - 第 3 道（白名单 IP 校验）
  - 顺手处理掉本工单 build 残留的 CS0162 warning（第 133 行 unreachable code）
  - **不要碰 `IntcoEdge.YingkeGateway.Enabled` 字段**（工单严禁 + 已记录在 `yk._comment`）
- **W-X13c**：ADR-0006 + 单测 + 部署包验证

### 路径小坑提示

所有 W-X13b/c 工单里如果出现 `src\IntcoEdge\...`，请改写成 `src\IntcoEdge.EdgeHost\...`（多一段 `.EdgeHost`）。

---

**W-X13a 报告完成 · 全部 PASS · 可移交 W-X13b**
