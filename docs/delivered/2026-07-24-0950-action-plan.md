# W-X29-action: 老板指令边界对齐（PM 自写，未派工）

> **PM 自写**。老板 09:50 三件事，先对齐边界再动手。

## 老板指令原文

> 1. 把老的杀掉
> 2. 新的我们先不推送，只做本地测试
> 3. 计算一下，十分钟内能推送多少条数据，然后给我统计结果

## PM 摸清的事实（关键）

### 1. "老的"在哪？本机没找到，PM 杀不了

| 项 | 状态 |
|---|---|
| 老板本机 80 端口 LISTEN | hik-java PID 27548（DataupLoad 新服务）|
| 老 PSM 残留（本机磁盘）| D:\IntcoEdge（7/20 前的 C# EdgeHost，已废，DB 文件 7/20 后无更新）|
| **192.168.135.15:80** | **reachable=True** ← 老 PSM 在**另一台机器** |
| **192.168.135.15:443** | **reachable=True** ← 同上 |
| ARP 表 192.168.135.15 | MAC 98-df-82-80-d0-04（**不是本机网卡**）|

**PM 摸不到 192.168.135.15**：
- PM 没有那台机器的 RDP / WinRM 凭据
- 防火墙没配远程管理
- 远程杀进程需要那台机的本地账号

**老板选项**：
- 🅐 老板自己 RDP 到 192.168.135.15 停服务（最快）
- 🅑 PM 跑 `Stop-Service` / `taskkill` 试一下，**可能失败需要账号**
- 🅒 老板拉个临时远程会话共享给 PM
- 🅓 跑 `Get-Service` 探测它的服务名，再决定怎么停

### 2. "新的不推送" — 已经在做 ✅

| 项 | 状态 |
|---|---|
| application-prod.yml `yk.uploadEnabled` | **false**（铁则 42，从 7/22 21:23 守住至今）|
| hik-java PID 27548 outbound 到 MES | **0 条 TCP** |
| sendAlarmMessage → yk.push 调用 | 0 次（被 isIgnore SQL 异常阻断）|

**红线守住中，不需要改**。

### 3. "十分钟能推送多少条" — PM 需要老板拍 2 件事才能给准数

**现在根本推不出去**（isIgnore SQL 异常阻断），要给老板准确的"10 分钟应该推多少"数字，必须：

#### 路径 🅐：**修 sendAlarmMessage 那个 SQL bug**（isIgnore 第 85 行）

- 改 `IgnoreAlarmMapper` SQL：`end_time > ?` → `end_time > ?::varchar`（同 W-X23c 做法）
- 不开推送（uploadEnabled 仍 false）
- 跑 10 分钟 graybox
- 统计 `would_push_unsolved`（应用层判定应推但实际没推）

**预估 10 分钟数字**：基于 9-10 点 572 条 type=1 缺陷报警，约 95 条/10 分钟（按 PSM 规则 22/h 应推，灰盒期）

#### 路径 🅑：**临时开推送 + 修 SQL bug**

- 改 yk.uploadEnabled → true
- 修 isIgnore SQL
- 跑 10 分钟
- 统计实际推送数 + 飞书群接收数

**风险**：飞书群再次刷屏（但老板已经要"看 10 分钟数据"），需要老板明确接受。

#### 路径 🅒：**只统计应用层判定应推的（最安全）**

- 不开推送、不修 SQL
- 跑 10 分钟，看 log 里 `current alarm is interesting defect` 的报警数（被 isIgnore 抛异常前被应用层判定的）
- 给老板一个**理论应推数**，不是实际推送数

**PM 建议**：先 🅐 修 SQL + 跑 10 分钟统计 `would_push_unsolved`，再决定是否走 🅑。

### 4. PM 现在能立即做的事（不需老板拍）

- ✅ 跑 cron 监控 30s 间隔，盯 DataupLoad.log 是否恢复（logback 卡死问题）
- ✅ 探 192.168.135.15 上跑的是什么服务名（也许能远程停）
- ✅ 准备 W-X30 工单（修 isIgnore SQL）

## 老板拍板 3 件事

| # | 选项 | PM 默认 |
|---|---|---|
| 1. 杀老 PSM | 🅐 老板 RDP 去停 / 🅑 PM 试远程 Stop-Service / 🅒 老板开远程给 PM | 🅐（最稳）|
| 2. 修 SQL + 跑 10 分钟 | 🅐 修 isIgnore SQL + 跑 10min graybox（不开推送）/ 🅑 修 SQL + 开推送 + 跑 10min / 🅒 只跑理论统计 | 🅐（最安全）|
| 3. 修老 PSM 杀不掉时的兜底 | 切断 192.168.135.15 的网络（物理断网 / 防火墙封）/ 改 MES 黑名单 / 容忍 | 🅓 等老板拍 |

PM 等老板回复。
