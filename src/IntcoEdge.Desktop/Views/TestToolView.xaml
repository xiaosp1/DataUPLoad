<UserControl x:Class="IntcoEdge.Desktop.Views.TestToolView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="🧪 测试工具 — 模拟视觉软件打 EdgeHost REST API"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>

        <!-- 服务地址 -->
        <GroupBox Grid.Row="1" Header="EdgeHost 地址" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Base URL：" VerticalAlignment="Center"/>
                <TextBox  Grid.Column="1" x:Name="BaseUrlBox" VerticalAlignment="Center"
                          Text="http://127.0.0.1:5288"/>
                <Button   Grid.Column="2" Content="应用" Click="OnApplyBaseUrl"/>
                <Button   Grid.Column="3" Content="🔌 Ping 端口" Click="OnPing"/>
                <TextBlock Grid.Column="4" x:Name="PingStatusText" Text="未测试" Margin="8,0,0,0" VerticalAlignment="Center"/>
            </Grid>
        </GroupBox>

        <!-- 三个测试按钮 -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TabControl Grid.Row="0" x:Name="ApiTabs">
                <!-- 测试 1: POST /client/data/detect -->
                <TabItem Header="① POST /client/data/detect（推检测数据）">
                    <Grid Margin="6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="faceNo（面）：" VerticalAlignment="Center"/>
                        <TextBox  Grid.Row="0" Grid.Column="1" x:Name="DetectFaceNo" Text="A1"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="lineNo（线）：" VerticalAlignment="Center"/>
                        <TextBox  Grid.Row="1" Grid.Column="1" x:Name="DetectLineNo" Text="L01"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="todayData 检测数：" VerticalAlignment="Center"/>
                        <TextBox  Grid.Row="2" Grid.Column="1" x:Name="DetectTodayCount" Text="1200"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="todayData 异常数：" VerticalAlignment="Center"/>
                        <TextBox  Grid.Row="3" Grid.Column="1" x:Name="DetectTodayDefect" Text="3"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="realTimeData 缺陷：" VerticalAlignment="Center"/>
                        <TextBox  Grid.Row="4" Grid.Column="1" x:Name="DetectRealtimeDefect" Text="底面破损"/>

                        <TextBlock Grid.Row="5" Grid.Column="0" Text="realTimeData 数量：" VerticalAlignment="Center"/>
                        <TextBox  Grid.Row="5" Grid.Column="1" x:Name="DetectRealtimeCount" Text="1"/>

                        <TextBlock Grid.Row="6" Grid.Column="0" Text="时间戳：" VerticalAlignment="Center"/>
                        <TextBox  Grid.Row="6" Grid.Column="1" x:Name="DetectTimestamp" Text="2026-07-20 14:30:00"/>

                        <CheckBox Grid.Row="7" Grid.ColumnSpan="2" x:Name="UseRawJson"
                                  Content="使用下方原始 JSON（不构建对象）" Margin="4"/>

                        <TextBlock Grid.Row="8" Grid.ColumnSpan="2" Text="请求体预览（自动构建）：" FontWeight="Bold"/>
                        <TextBox  Grid.Row="9" Grid.ColumnSpan="2" x:Name="DetectPayloadPreview" IsReadOnly="True"
                                  Height="80" FontFamily="Consolas" FontSize="11"
                                  VerticalScrollBarVisibility="Auto"
                                  AcceptsReturn="True" TextWrapping="NoWrap"/>

                        <StackPanel Grid.Row="10" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,6,0,0">
                            <Button Content="🔨 重建预览" Click="OnBuildDetectPayload"/>
                            <Button Content="📤 POST 发送" Click="OnPostDetect" Background="#1976D2" Foreground="White" FontWeight="Bold"/>
                            <Button Content="📥 查 PG 入库结果" Click="OnCheckDbResult"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <!-- 测试 2: GET /client/yk/line-defect -->
                <TabItem Header="② GET /client/yk/line-defect（拉字典）">
                    <Grid Margin="6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="无请求参数，点击下面按钮拉取字典数据。"
                                   Margin="4" Foreground="#666"/>
                        <StackPanel Grid.Row="1" Margin="4">
                            <Button Content="📤 GET 发送" Click="OnGetLineDefect"
                                    Background="#1976D2" Foreground="White" FontWeight="Bold"
                                    HorizontalAlignment="Left"/>
                        </StackPanel>
                        <TextBlock Grid.Row="2" Text="响应将显示在下方" Margin="4" Foreground="#666"/>
                    </Grid>
                </TabItem>

                <!-- 测试 3: POST /client/yk/defect-record -->
                <TabItem Header="③ POST /client/yk/defect-record（拉缺陷记录）">
                    <Grid Margin="6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="time（整点）：" VerticalAlignment="Center"/>
                        <TextBox  Grid.Row="0" Grid.Column="1" x:Name="DrTime" Text="2026-07-20 14:00:00"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="lindGroup（逗号）：" VerticalAlignment="Center"/>
                        <TextBox  Grid.Row="1" Grid.Column="1" x:Name="DrLindGroup" Text="L01,L02"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="defectGroup（逗号）：" VerticalAlignment="Center"/>
                        <TextBox  Grid.Row="2" Grid.Column="1" x:Name="DrDefectGroup" Text=""/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="faceGroup（逗号）：" VerticalAlignment="Center"/>
                        <TextBox  Grid.Row="3" Grid.Column="1" x:Name="DrFaceGroup" Text=""/>

                        <TextBlock Grid.Row="4" Grid.ColumnSpan="2" Text="请求体预览（自动构建）：" FontWeight="Bold"/>
                        <TextBox  Grid.Row="5" Grid.ColumnSpan="2" x:Name="DrPayloadPreview" IsReadOnly="True"
                                  Height="80" FontFamily="Consolas" FontSize="11"
                                  VerticalScrollBarVisibility="Auto" AcceptsReturn="True" TextWrapping="NoWrap"/>
                        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,6,0,0">
                            <Button Content="🔨 重建预览" Click="OnBuildDefectRecordPayload"/>
                            <Button Content="📤 POST 发送" Click="OnPostDefectRecord"
                                    Background="#1976D2" Foreground="White" FontWeight="Bold"/>
                        </StackPanel>
                    </Grid>
                </TabItem>
            </TabControl>

            <!-- 响应区 -->
            <GroupBox Grid.Row="1" Header="响应 / 日志" Margin="0,8,0,0" Height="220">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBlock Text="状态码：" VerticalAlignment="Center"/>
                        <TextBlock x:Name="RespStatusText" Text="—" FontWeight="Bold" VerticalAlignment="Center"/>
                        <Button Content="🗑 清空" Margin="20,0,0,0" Click="OnClearResponse"/>
                        <Button Content="📋 复制响应" Click="OnCopyResponse"/>
                    </StackPanel>
                    <TextBox Grid.Row="1" x:Name="ResponseBox" IsReadOnly="True"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto"
                             FontFamily="Consolas" FontSize="11"
                             AcceptsReturn="True" TextWrapping="NoWrap"
                             Background="#FAFAFA"/>
                </Grid>
            </GroupBox>
        </Grid>

        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock x:Name="FooterText" Text="就绪"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
