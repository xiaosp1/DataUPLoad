<UserControl x:Class="IntcoEdge.Desktop.Views.WatchdogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="🛡️ 看门狗 — EdgeHost 主进程监控"
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>

        <!-- 目标配置 -->
        <GroupBox Grid.Row="1" Header="目标配置" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="EdgeHost 路径：" VerticalAlignment="Center"/>
                <TextBox  Grid.Column="1" x:Name="ExePathBox" VerticalAlignment="Center"
                          Text="D:\IntcoEdge\edge-v0.3\IntcoEdge.EdgeHost.exe"/>
                <Button   Grid.Column="2" Content="浏览..." Click="OnBrowseExe"/>
                <Button   Grid.Column="3" Content="应用" Click="OnApplyExe"/>
            </Grid>
        </GroupBox>

        <!-- 状态 -->
        <GroupBox Grid.Row="2" Header="状态" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="状态：" FontWeight="Bold" VerticalAlignment="Center"/>
                <Border Grid.Row="0" Grid.Column="1" x:Name="StateBadge" CornerRadius="4" Padding="6,2"
                        Background="#999" Margin="4,0">
                    <TextBlock x:Name="StateText" Text="未知" Foreground="White" FontWeight="Bold"/>
                </Border>

                <TextBlock Grid.Row="0" Grid.Column="2" Text=" 累计重启：" VerticalAlignment="Center"/>
                <TextBlock Grid.Row="0" Grid.Column="3" x:Name="RestartCountText" Text="0" FontWeight="Bold"
                           VerticalAlignment="Center"/>

                <TextBlock Grid.Row="0" Grid.Column="4" Text=" 最后检查：" VerticalAlignment="Center" Margin="8,0,0,0"/>
                <TextBlock Grid.Row="0" Grid.Column="5" x:Name="LastCheckText" Text="—"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Row="1" Grid.ColumnSpan="6" Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="▶ 启动看门狗" Click="OnStart" x:Name="StartBtn"/>
                    <Button Content="■ 停止看门狗" Click="OnStop" x:Name="StopBtn"/>
                    <Button Content="▶▶ 手动启动一次" Click="OnManualStart" x:Name="ManualBtn"/>
                    <Button Content="查 PID" Click="OnRefresh" x:Name="RefreshBtn"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- 日志 -->
        <GroupBox Grid.Row="3" Header="日志">
            <TextBox x:Name="LogBox" IsReadOnly="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas" FontSize="12"
                     Background="#FAFAFA"/>
        </GroupBox>

        <!-- 底部状态栏 -->
        <StatusBar Grid.Row="4">
            <StatusBarItem>
                <TextBlock x:Name="FooterText" Text="就绪"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
