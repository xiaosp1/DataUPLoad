<Window x:Class="IntcoEdge.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IntcoEdge.Desktop"
        xmlns:views="clr-namespace:IntcoEdge.Desktop.Views"
        mc:Ignorable="d"
        Title="英科边缘数据桌面工具 v0.3"
        Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        FontFamily="Microsoft YaHei UI, Segoe UI"
        FontSize="13">
    <DockPanel>
        <!-- 顶部信息栏 -->
        <Border DockPanel.Dock="Top" Background="#1976D2" Padding="12,8">
            <StackPanel>
                <TextBlock Text="IntcoEdge Desktop v0.3" Foreground="White"
                           FontSize="16" FontWeight="Bold"/>
                <TextBlock Text="看门狗 / SQLite 浏览器 / 测试工具" Foreground="#E0E0E0"
                           FontSize="11" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <!-- 底部状态栏 -->
        <StatusBar DockPanel.Dock="Bottom" Background="#F5F5F5">
            <StatusBarItem>
                <TextBlock x:Name="FooterText" Text="就绪 — 当前用户：admin"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="TimeText" Text=""/>
            </StatusBarItem>
        </StatusBar>

        <!-- 主体：TabControl -->
        <TabControl Margin="4" x:Name="MainTabs">
            <TabItem Header="🛡️ 看门狗">
                <views:WatchdogView/>
            </TabItem>
            <TabItem Header="🗄️ SQLite 浏览器">
                <views:PgClientView/>
            </TabItem>
            <TabItem Header="🧪 测试工具">
                <views:TestToolView/>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
